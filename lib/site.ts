/** Canonical origin and name, shared by metadata, robots.txt and the sitemap. */

/**
 * TODO: still the old reader's deployment. Social previews and the canonical
 * URL resolve against this, so point it at wherever Wonders and Hope is
 * actually hosted before sharing any links.
 */
export const SITE_URL = 'https://bible-new-testament.vercel.app'

export const SITE_NAME = 'Wonders and Hope'
